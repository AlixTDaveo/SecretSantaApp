package com.example.secretsanta.ui.theme

import androidx.compose.ui.graphics.Color

object ChristmasColors {
    // Couleurs Auth (Login/Register)
    val AuthBackground = Color(0xFF006054)      // Vert foncé fond
    val AuthButtonRed = Color(0xFFEA2A32)       // Rouge bouton

    // Couleurs App principale
    val AppBackground = Color(0xFF0D9268)       // Vert clair fond
    val AppButtonRed = Color(0xFFEA2A34)        // Rouge bouton
    val SkyBlue = Color(0xFF8DC5D9)             // Bleu ciel

    // Couleurs Messagerie
    val MessageBackground = Color(0xFFDEEEE5)   // Vert pâle

    // Utilitaires
    val White = Color(0xFFFFFFFF)
    val SnowWhite = Color(0xFFFFFBF5)
    val DarkText = Color(0xFF1C1B1F)
}